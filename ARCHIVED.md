# 아카이브됨 (2026-08-13)

이 리포지터리는 **폐기된 프로젝트**입니다. 참조용으로만 남깁니다.

- 수집 워크플로와 GitHub Pages 배포를 **중지**했습니다.
- 커밋 히스토리를 **마지막 상태 1개로 정리**했습니다.
- 데이터의 **이름은 마스킹**했습니다(`김기엽` → `김O엽`).
  `mbrId`는 원래부터 되돌릴 수 없는 sha1 마스킹본입니다.
- **엔드포인트 주소·경로는 코드에서 제거**하고 Actions Secret 참조로 바꿨습니다.
  값이 없으면 동작하지 않습니다.

## 필요한 Secret (동작시키려면)

| 이름 | 내용 |
|---|---|
| `CODYSSEY_API_BASE` | API 호스트 |
| `CODYSSEY_SECOM_PATH` | 출입기록 경로 (Jail) |
| `CODYSSEY_GUILD_PATH` | 길드 경로 (Jail) |
| `CODYSSEY_SESSION` | 로그인 세션 |

## 후속 프로젝트

이 트래커 3종은 통합 앱으로 대체되었습니다. 코드를 참조만 했을 뿐
런타임 의존은 모두 끊었습니다.
